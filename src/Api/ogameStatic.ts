import { ResourceList } from "./gameTypes";

// const getStaticProduction = (id: number, level: number) => {
// }
// export function getProduction(id: number, gameSpeed: number, level: number) {
//    return;
// }

export function getCosts(id: number, level: number): ResourceList | undefined {
    return undefined;
}
